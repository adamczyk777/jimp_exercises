//
// Created by adamc on 04.04.2017.
//

#include "StudentRepository.h"

istream &StudentRepository::operator>>(istream &is, StudentRepository &repository) {
    return <#initializer#>;
}

ostream &StudentRepository::operator<<(ostream &os, StudentRepository &repository) {
    return <#initializer#>;
}
